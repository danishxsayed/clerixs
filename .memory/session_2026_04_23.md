# Session Log: April 23, 2026 — Global Voice & Subscription Fulfillment

## 🎙️ Global Voice Input (Push-to-Talk)
Successfully implemented a cross-platform voice-to-text system optimized for Indian English.

- **System Architecture**: Switched from per-field toggles to a single, global floating PTT button (bottom-right).
- **React Sync Fix**: Overcame React state update issues where Web Speech API was "bypassing" React. Used native DOM property descriptors to manually trigger `input` events, ensuring form validation and state-binding work perfectly.
- **Cross-Platform UI**: Implemented OS-aware keyboard shortcut hints:
  - **macOS**: "Control + Shift"
  - **Windows/Linux**: "Ctrl + Shift"
- **Shortcut Handling**: Integrated `Spacebar` (hold) and the global keyboard combination into the recognition engine.

## 💳 Subscription & Payment Hardening
Resolved critical blockers preventing users from upgrading from the default 7-day trial to paid plans.

- **Upgrade Unblocking**: Adjusted the `create-order` API to distinguish between "Active Trial" and "Active Paid" plans, allowing trial users to purchase immediately without conflict errors.
- **Robust Fulfillment (Pending Orders)**:
  - Created `pending_orders` table to store `plan_id` and `org_id` *before* hitting the payment gateway.
  - This solves the "Missing Tags" issue in Cashfree Sandbox where metadata is often lost in redirects.
- **Persistent Billing History**:
  - Implemented `subscription_invoices` table to store successful transaction records permanently.
  - Updated the Subscription Dashboard to fetch from this table, replacing the previous "last-only" payment view.
- **Cache Invalidation**:
  - Added aggressive `revalidatePath` calls for both Layout and Page.
  - Implemented cache-busting timestamps (`&t=...`) on the verification fetch to force browsers to see the live DB status instantly after payment.

## 📂 Database Schema Updates
- **Migration 00050**: Added `pending_orders` and `subscription_invoices` with RLS policies ensuring secure access to billing history for clinic owners.

## 📍 Environment Context
- **Cashfree Mode**: Confirmed functionality for both `sandbox` and `production` based on `CASHFREE_ENV`.
- **Admin Privileges**: Ensured all fulfillment logic uses the `adminSupabase` client to bypass RLS during critical system-level updates.
